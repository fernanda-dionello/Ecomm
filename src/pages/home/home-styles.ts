import { css } from "@emotion/react"

export const container = () => {
    return css`
        gap: 2rem;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-wrap: wrap;
    `;
}
